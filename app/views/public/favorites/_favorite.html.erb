<% if item.favorited_by?(current_customer) %>
  <%= link_to item_favorites_path(item), method: :delete, remote: true do %>
    <p>♥<%= item.favorites.count %> いいね</p>
  <% end %>
<% else %>
  <%= link_to item_favorites_path(item), method: :post, remote: true do %>
    <p>♡<%= item.favorites.count %> いいね</p>
  <% end %>
<% end %>