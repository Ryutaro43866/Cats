<div class="container">
  <h1>投稿詳細</h1>
  <div class="row">
    <div>
      <p><%= @customer.name %></p>
      <p><%= @item.title %></p>
      <p><%= @item.introduction %></p>
      <p><%= @item.created_at.strftime('%Y/%m/%d') %></p>
    </div>
  </div>
</div>
